package eco

import "core:fmt"


/// economical component object
/// .eco format is a dsl and file format for interoping with the kernel/FFI for both C and Odin; as well as exposing the file format to swift.
/// .ecodb is the same, but for a minimal database on the game object level.
